return {
  "saghen/blink.cmp",
  opts = {
    keymap = { preset = "default" },
    completion = {
      -- I'm sceptical about whether I want this or not
      accept = {
        auto_brackets = {
          enabled = false,
        },
      },
      ghost_text = { enabled = false },
    },
  },
}
